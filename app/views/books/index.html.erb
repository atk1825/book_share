<h1>トップページ</h1>
<div class="">
<%= link_to "投稿する", new_book_path %>
</div>
<% @books.each do |book| %>
<ul class="item_list">
  <li>
    <%= image_tag book.image, class: 'book-image' %><br>
    <%= link_to book.title, book_path(book) %>
  </li>
</ul>
<% end %>

<%= link_to current_user.nickname, users_path(current_user) %>